apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-deployment
  namespace: primos-system
  labels:
    app: worker
spec:
  replicas: 3  # 3 workers para demostrar distribución de carga
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - name: worker
        image: jemartinez02/worker:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: primos-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "150m"
# No Service - los workers no exponen puertos, solo consumen de la cola
